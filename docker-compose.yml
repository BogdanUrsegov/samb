services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      PYTHONUNBUFFERED: "1"
      PYTHONPATH: /app
      DATABASE_URL: sqlite+aiosqlite:////app/data/database.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
